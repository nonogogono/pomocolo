<% if @feed_items.any? %>
  <ol class="microposts">
    <% @feed_items.each do |feed_item| %>
      <%= render 'shared/micropost', micropost: feed_item %>
    <% end %>
  </ol>
  <%= paginate @feed_items, params: { controller: :static_pages, action: :home} %>
<% end %>
